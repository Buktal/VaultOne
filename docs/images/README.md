# Images

README 引用的截图放在此目录。打 `v1.0.0` tag 前请补齐以下两张（文件名需与 README 引用一致）：

| 文件名 | 内容 | 建议尺寸 |
| --- | --- | --- |
| `dashboard.png` | 完整看板：顶部统计卡（token 四桶 / 缓存命中率 / 请求数 / 成本）+ 趋势图 + 请求日志 | 宽 1200px 左右，PNG |
| `lightweight.png` | 轻量速览模式：贴边半图标 + 悬停展开的今日用量速览 | 宽 800px 左右，PNG |

中英两份 README 共用同一份图片，无需分别维护。

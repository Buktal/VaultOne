# Images

README 顶部的截图。各提供浅色 / 深色两份，README 用 `<picture>` + `prefers-color-scheme` 按访客主题自动切换。

| 文件 | 内容 |
| --- | --- |
| `dashboard-light.png` / `dashboard-dark.png` | 完整看板：统计卡（token 四桶 / 缓存命中率 / 请求数 / 成本）+ 趋势图 + 请求日志 |
| `lightweight-light.png` / `lightweight-dark.png` | 轻量速览模式：贴边半图标 + 悬停展开的今日用量速览 |

中英两份 README 共用同一份图片。替换截图时保持文件名与 README 引用一致。

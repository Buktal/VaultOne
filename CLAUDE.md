# CLAUDE.md

## Agent skills

### Issue tracker

工作记录（issues、PRD）以 GitHub Issues 跟踪，所有操作走 `gh` CLI。详见 `docs/agents/issue-tracker.md`。

### Domain docs

单上下文（single-context）布局：仓库根目录的 `CONTEXT.md` 与 `docs/adr/`。详见 `docs/agents/domain.md`。
